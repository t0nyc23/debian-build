# Debian-build

Automate the boring process of setting up my desktop after a fresh Debian installation.

Execute the following:
`chmod +x build.sh && ./build.sh`

Reboot after the scripts have finished the setup.

